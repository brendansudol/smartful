name: smartful-daily-email
on:
  schedule:
    - cron: "*/15 * * * *"
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call API route to... (TODO)
        run: |
          curl --request GET \
          --url 'https://getsmartful.com/api/testAuth' \
          --header 'Authorization: Bearer ${{ secrets.SMARTFUL_AUTH_KEY }}'
